# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'
use_frameworks!
target 'ben quotes' do
  # Comment the next line if you're not using Swift and don't want to use dynamic
  # Pods for ben quotes
pod "SwiftCSV" 

end
